# 麻將 2 代規劃整理

> 文件狀態：討論整理、技術規劃與已完成的 2 代第一階段實作紀錄；1 代與既有後端仍維持不變。
>
> 整理日期：2026-09-06

## 1. 目標

麻將 2 代的目標，是讓玩家在 PC、iPad 與其他平板裝置上，感受到接近真人打麻將的牌桌視覺與操作節奏：

- 開局流程有完整的抽位、砌牌牆、發牌與整理手牌過程。
- 麻將牌有實體感、厚度、旋轉、陰影與立體光影。
- 摸牌、出牌、吃、碰、槓、胡都能從原位置自然移動到新位置。
- 上家、對家、下家與自己的牌，依座位方向自然轉向與排列。
- 棄牌區維持牌局順序，並且位於牌桌中央。
- PC、全螢幕、iPad 全螢幕與非全螢幕都能維持合理的牌距與比例。
- 線上同步發生延遲或重新連線時，畫面不會突然跳牌、重排或倒退。

## 2. 參考網站

本次討論以 [逗陣打麻將](https://letsmj.com/) 首頁的「馬上開打」流程作為體驗參考。

實際觀察到的流程：

1. 首頁可以直接進入公開牌桌。
2. 等待真人配對時，可以選擇等待或直接開打。
3. 直接開打後，先出現「抽位」動畫。
4. 抽位完成後進入牌桌，牌牆、各家手牌、棄牌區與中央風位資訊逐步出現。
5. 輪到自己時，手牌、摸牌、出牌提示與計時提示各自位於固定的牌桌層級。

## 3. 參考網站目前可確認的技術

以下是從公開首頁、瀏覽器載入資源與實際牌局流程確認到的內容。網站的原始碼沒有公開，因此「使用的伺服器語言」與完整內部模組名稱不能百分之百確定。

### 前端與打包

- 前端使用 React。
- 使用 Vite 類型的 hashed module bundle 打包。
- HTML 中可看到 React/TSX 專案留下的檔案註記，例如 `InfoPages.tsx` 與 `apps/server/src/seo/shell.ts`。
- 目前抓到的主要資源：
  - [JS bundle](https://letsmj.com/assets/index-B1SmsBzE.js)
  - [CSS bundle](https://letsmj.com/assets/index-BrcMxU0Y.css)
  - [PWA manifest](https://letsmj.com/manifest.webmanifest)

### 牌桌與手牌渲染

- bundle 中包含 Three.js r170。
- bundle 中包含 `@react-three/fiber`。
- 玩家手牌位於 `handscene`，使用 WebGL canvas 與正交攝影機渲染。
- 麻將牌不是單純的平面圖片，而是由 3D mesh、材質、光源與旋轉組成。
- 牌牆、手牌、摸牌、吃碰槓牌、棄牌與胡牌牌面，都透過座位和牌的狀態計算 3D 位置。

### 線上同步

- bundle 中包含 Socket.IO client。
- Socket.IO 設定使用 WebSocket transport。
- 連線時會帶入玩家 ID、版本、登入資訊、房號與畫面資訊。
- 因此可以確認前端是以即時 socket 事件同步牌局；後端很可能採用 Node.js／Socket.IO，但後端語言本身仍屬合理推測，不能視為已公開確認。

### CSS 與 PWA

- CSS 負責 HUD、提示、按鈕、吃碰槓胡特效、浮動提示與部分轉場動畫。
- 使用自訂麻將字型與 Noto Sans TC／Noto Serif TC。
- manifest 設定為全螢幕與橫向顯示，符合平板牌桌使用情境。

## 4. 為什麼畫面會自然順暢

自然感並不是單靠「動畫時間縮短」產生，而是由以下幾個設計共同造成。

### 4.1 牌是有身份的物件，不是單純牌面圖片

每張牌需要維持唯一 ID，並知道自己目前位於哪個區域：

- 牌牆
- 某一家的手牌
- 摸到的牌
- 棄牌區
- 吃、碰、槓牌組
- 花牌或胡牌展示區

當一張牌從手牌打出時，視覺層可以知道它的起點與終點，因此能做「從手上飛到棄牌區」的連續動畫，而不是先刪除再重新產生一張牌。

### 4.2 使用牌桌世界座標

牌桌不是用四組互不相關的 CSS card 排版，而是把每一個座位轉成同一個牌桌座標系：

- 對家旋轉 180 度。
- 上家與下家旋轉 90 度或 -90 度。
- 手牌依照座位方向、牌數、摸牌位置與吃碰槓數量計算。
- 棄牌依照牌序、欄數與列數計算。
- 牌牆依照牆段和牌的位置計算。

這種方式能讓同一張牌在不同動作之間保持空間連續性。

### 4.3 3D mesh、光線和陰影

立體牌面提供了幾個純 CSS 平面牌很難自然產生的效果：

- 牌的厚度與邊緣。
- 牌面向上時的光線反射。
- 牌背與牌牆的遮擋關係。
- 牌被抬起時的陰影變化。
- 不同座位的牌面旋轉與透視。

因此玩家看到的不是「圖片換位置」，而是像一張實體牌被摸起、放下或推到桌面上。

### 4.4 使用動畫時間軸

牌局動作具有明確的階段，而不是所有狀態同一幀更新：

1. 擲骰或抽位。
2. 顯示莊家結果。
3. 洗牌或牌牆準備。
4. 發牌。
5. 整理手牌。
6. 摸牌。
7. 出牌或吃碰槓。
8. 顯示結果。

參考網站的抽位動畫會以短間隔滾動座位風向，約 1.2 秒後固定結果。吃、碰、槓、胡與放槍也使用不同的持續時間與特效。

### 4.5 Spring、easing 與 CSS 特效分工

- 選牌時使用 spring-like 的上浮、放大與回彈。
- 拖曳出牌時即時跟隨指標，放開後才確認動作。
- 牌移動使用 cubic-bezier easing，而不是單純 linear。
- 吃碰槓胡使用 CSS overlay、光圈、閃光、文字與粒子效果。
- `prefers-reduced-motion` 使用者可以減少部分動畫。

3D 場景負責牌和桌面的空間移動，CSS overlay 負責提示和戲劇性效果，兩者分工可以兼顧效能與開發速度。

### 4.6 不同裝置使用不同攝影機 profile

參考網站有 desktop、mobile-landscape、mobile-portrait 等不同設定，會分別調整：

- 攝影機位置。
- 攝影機縮放。
- 對家與側家手牌深度。
- 棄牌起始位置、比例、列數與欄數。
- 玩家手牌與操作區高度。

因此它不是把 PC 版面整張縮小，而是針對裝置重新取景。

## 5. 「100% 達到」的定義

### 可以做到的部分

如果「100%」是指使用者感受到的效果，包括：

- 3D 麻將桌。
- 牌面與牌背的立體感。
- 自然的摸牌、出牌、吃碰槓胡動畫。
- 四方位座位與棄牌區。
- PC、iPad、全螢幕和非全螢幕的自適應。
- 線上牌局的即時同步與動畫播放。

這些在目前的 Web 技術上都可以實作，技術上可達到同等級，甚至依照自己的需求做得更完整。

### 無法保證完全相同的部分

以下項目不能保證與參考網站逐像素或逐毫秒相同：

- 對方未公開的原始模型和材質。
- 對方實際使用的動畫曲線與時間參數。
- 對方內部的事件排程與伺服器補償邏輯。
- 不同裝置的 GPU、瀏覽器與 WebGL 效能差異。
- 對方的音效、語音與美術資產。

麻將 2 代應該以「自行建立相同等級的體驗」為目標，而不是複製對方的原始碼、素材或品牌視覺。

## 6. 麻將 2 代建議架構

目前專案的遊戲規則、房間與多人同步不需要全部重寫，建議保留資料層，新增一個獨立的牌桌渲染層。

### 建議保留

- 麻將規則與牌型判定。
- 吃、碰、槓、胡的合法動作。
- 玩家、房間與座位資料。
- Firebase 房間同步與既有連線流程。
- 線上 2P、線上 4P 的同步協議。
- 單機模式的 bot 與局面產生。
- 現有 HUD、登入、PWA 與 Android 包裝流程。

### 建議新增

```text
src/table3d/
  TableScene              3D canvas、桌面、牌牆、燈光、攝影機
  TileWorldModel          tile id、zone、seat、index、meld index
  SeatLayoutProfiles      desktop、mobile-land、mobile-port
  TablePositionMapper     將遊戲狀態轉成世界座標
  AnimationTimeline       發牌、摸牌、出牌、吃碰槓、胡牌動畫
  RemoteEventAdapter      將線上事件轉成視覺事件
  TableInputController    點牌、拖牌、觸控與 hover
  TableOverlay             HUD、提示、倒數、聊天與結果
```

### 核心資料概念

每張牌的視覺狀態至少要包含：

```ts
type TileVisualState = {
  uid: string;
  kind: number;
  seat: number;
  zone: "wall" | "hand" | "discard" | "meld" | "flower" | "win";
  index: number;
  meldIndex?: number;
  faceDown?: boolean;
  drawn?: boolean;
};
```

動畫系統需要同時保留上一個狀態和下一個狀態，才能知道牌要從哪裡移到哪裡：

```text
previous visual state
        ↓
event / animation timeline
        ↓
next visual state
        ↓
rendered 3D scene
```

## 7. 建議開發階段

### Phase 0：確立規格與效能基準

- 決定牌面、牌背、桌布、座位資訊與 HUD 視覺規格。
- 定義 PC、iPad 橫向、iPad 全螢幕的目標尺寸。
- 設定桌面與平板的效能門檻。
- 先確認 Three.js／React Three Fiber 是否納入正式 bundle。

### Phase 1：靜態 3D 牌桌

- 建立桌面、牌牆、四方座位與中央棄牌區。
- 顯示真實遊戲狀態，但先不做複雜動畫。
- 完成 PC、iPad 與全螢幕取景。

### Phase 2：玩家手牌與基本動作

- 玩家手牌 3D mesh。
- 摸牌上浮。
- 選牌上浮與回彈。
- 點牌與拖曳出牌。
- 牌從手牌移動到中央棄牌區。

### Phase 3：完整開局與牌局動畫

- 抽位動畫。
- 砌牌牆與發牌動畫。
- 整理手牌動畫。
- 吃、碰、槓的牌組排列動畫。
- 胡牌、放槍、結算特效。

### Phase 4：線上同步整合

- 將 Firebase 或現有同步事件轉成視覺事件。
- 區分「伺服器已確認狀態」與「本地播放中的動畫」。
- 防止延遲造成牌面倒退或重複播放。
- 驗證 host、guest、重新整理、重新連線與斷線恢復。

### Phase 5：裝置與效能 QA

- PC 一般視窗。
- PC 全螢幕。
- iPad Safari 非全螢幕。
- iPad PWA 全螢幕。
- Android 平板 WebView／Capacitor。
- 觸控、拖曳、旋轉、safe-area 與瀏覽器 viewport。

### Phase 6：逐步切換

- 先保留舊牌桌作為 fallback。
- 以 query、feature flag 或 build variant 開啟 2 代牌桌。
- 先在單機模式驗證，再接線上 2P，最後接線上 4P。
- 觀察真實裝置效能與同步問題後，再取代舊版牌桌。

## 8. 主要風險

- WebGL 在 Safari、iPad 與低階 Android 裝置上的效能差異。
- 3D 牌模、牌面材質與字型會增加 bundle 和記憶體使用量。
- 觸控拖曳與瀏覽器頁面手勢可能互相干擾。
- 線上延遲時，動畫時間軸可能與伺服器狀態不同步。
- PWA service worker 可能快取舊版 JavaScript 或材質。
- 牌面、音效、角色圖與參考網站素材必須自行製作或確認授權。
- 若同時重做規則層、網路層和視覺層，除錯範圍會過大；應先保持遊戲邏輯不變。

## 9. 結論

麻將 2 代在技術上可以達到參考網站同等級的視覺與操作效果，但它本質上是「重做牌桌渲染與動畫層」，不是一次 CSS 微調。

最穩定的策略是：

1. 保留目前成熟的規則、房間和多人同步。
2. 以 Three.js／React Three Fiber 建立新的牌桌視覺層。
3. 用唯一牌 ID、座位座標與動畫時間軸，讓牌的移動具備物理連續性。
4. 先完成單機靜態牌桌，再做基本動作，再接線上同步。
5. 以 PC、iPad 和全螢幕實機結果作為驗收，而不是只看桌面瀏覽器縮放效果。

本文件後續可以作為麻將 2 代的產品規格、技術設計與開發拆解基準。

## 10. 第一個可玩的實作增量（2026-09-06）

目前已完成第一個可玩的 2 代牌桌增量，使用 query feature flag 開啟：

- 在首頁點選「試玩麻將 2 代」，或在網址加上 `?table=v2`。
- 沒有 `table=v2` 時維持 1 代，既有 1 代 markup、Firebase、房間資料格式與遊戲規則不變。
- 2 代使用 Three.js／React Three Fiber，並以現有 React bridge snapshot 作為唯一資料來源。
- 目前支援單機 2P、單機 4P、線上 2P 與線上 4P；玩家座位固定在下方，對家在上方，4P 時上家／下家在左右兩側。
- 棄牌集中在中央牌河，依各家方向排列；新增棄牌會由中央飛入，玩家手牌可直接點擊，吃、碰、槓、胡等既有 action command 仍沿用原本流程。
- 牌面沿用專案既有 SVG 牌稿並轉成 CanvasTexture，沒有新增外部牌面或角色素材依賴。

本增量先完成牌桌渲染、基本牌移動與操作接線；抽位、完整砌牌牆／發牌時間軸、進階特效與真機效能調校仍依 Phase 3～5 持續驗證。若裝置不適合 WebGL，可移除 query flag 回到 1 代。

## 11. 2 代視覺修正（2026-09-06）

- 輪到自己出牌的提示固定放在中央回合資訊與右下狀態區，不再放在玩家牌列上方造成遮擋。
- 牌牆與未公開手牌改用米白牌框、綠色內面與不同層次的牌背，避免被誤認為捲軸或除錯區塊。
- 牌面 CanvasTexture 以高解析度繪製，並使用攝影機遠近差、牌框光照與桌面漸層，提升 PC、平板與寬螢幕的辨識度。
- 2 代牌桌採用接近參考站的綠色絨面、木質桌框、中央風圈、座位資訊膠囊與回合金色提示；參考的是互動層次與空間配置，牌稿與角色素材仍使用本專案資產。
